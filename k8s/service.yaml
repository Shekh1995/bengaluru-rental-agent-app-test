apiVersion: v1
kind: Service
metadata:
  name: bengaluru-rental-agent-svc
  labels:
    app: bengaluru-rental-agent
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: bengaluru-rental-agent
